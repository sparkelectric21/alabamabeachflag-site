import test from "node:test";import assert from "node:assert/strict";import{readFileSync}from"node:fs";import{matchesReport,terminalStatus,updatePayload}from"../admin/information-reports/core.js";
const report={id:"r1",clientReportId:"c1",status:"new",category:"mapPinOrDirections",beachId:"orange-beach",mapPoiId:"poi-1",contextTitle:"Cotton Bayou",message:"Wrong entrance shown"};
test("report filters search stable IDs, titles, and messages",()=>{assert.equal(matchesReport(report,{search:"poi-1"}),true);assert.equal(matchesReport(report,{search:"cotton",status:"new",category:"mapPinOrDirections",beach:"orange-beach"}),true);assert.equal(matchesReport(report,{status:"resolved"}),false)});
test("status payload trims notes and handles duplicate links",()=>{assert.deepEqual(updatePayload("resolved","  checked  "),{status:"resolved",resolutionNote:"checked"});assert.deepEqual(updatePayload("duplicate",""," r2 "),{status:"duplicate",resolutionNote:null,duplicateOfReportId:"r2"});assert.equal(terminalStatus("dismissed"),true);assert.equal(terminalStatus("inReview"),false)});
test("report rendering uses textContent and authenticated same-origin routes",()=>{const html=readFileSync(new URL("../admin/information-reports/index.html",import.meta.url),"utf8"),source=readFileSync(new URL("../admin/information-reports/information-reports.js",import.meta.url),"utf8");assert.match(html,/noindex,nofollow,noarchive/);assert.match(source,/textContent=value/);assert.doesNotMatch(source,/innerHTML/);assert.match(source,/requestJson/)});
